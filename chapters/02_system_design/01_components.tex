\section{Components}

Furthermore, here is another example: if we want to reference an equation, \eqref{eq:incidencia_matrix_undirected} is the proper way to do it.

\begin{equation}
\label{eq:incidencia_matrix_undirected}
M = [m_{ij}], \quad \text{where} \quad
m_{ij} =
\begin{cases}
1, & \text{if } v_i \text{ is one of the endpoints of edge } e_j, \\
0, & \text{otherwise.}
\end{cases}
\end{equation}
\skipline
\gls{gpu} is faster than a \gls{cpu} for specific tasks. But not everyone has \gls{gpu}, and for specific tasks \gls{cpu} can be faster.

\bigskip


\lipsum[2-4]